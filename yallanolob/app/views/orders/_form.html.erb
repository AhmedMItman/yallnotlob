<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :resturant %>
    <%= form.text_field :resturant, id: :order_resturant %>
  </div>

  <div class="field">
    <%= form.label :menu %>
    <%= form.text_field :menu, id: :order_menu %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type, id: :order_type %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :order_status %>
  </div>

  <div class="field">
    <%= form.label :User_id %>
    <%= form.text_field :User_id, id: :order_User_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
